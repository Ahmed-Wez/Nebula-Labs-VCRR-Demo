dataset:
  name: core50
  num_tasks: 10
  classes_per_task: 5

training:
  model: resnet18
  epochs_per_task: 10
  batch_size: 64
  lr: 0.01
  optimizer: sgd
  momentum: 0.9
  weight_decay: 5e-4
  augment: true

method: lwf

distill:
  use_lwf: true
  alpha: 0.5
  temperature: 2.0