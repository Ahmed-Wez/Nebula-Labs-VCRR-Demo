dataset:
  name: cifar100
  num_tasks: 5 
  classes_per_task: 20

training:
  epochs_per_task: 3
  batch_size: 128
  lr: 0.01
  momentum: 0.9
  weight_decay: 5e-4

ewc:
  lambda_ewc: 1000.0

vcrr:
  reconfig_k: 8

logging:
  save_checkpoints: true
  log_interval: 50